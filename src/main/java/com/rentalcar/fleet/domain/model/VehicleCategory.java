package com.rentalcar.fleet.domain.model;

/**
 * Enum representing vehicle categories in the rental fleet
 */
public enum VehicleCategory {
    ECONOMY,
    COMPACT,
    MIDSIZE,
    FULLSIZE,
    SUV,
    LUXURY,
    VAN
}
